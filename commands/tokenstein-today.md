---
name: tokenstein-today
description: "Today's Claude token totals and per-model split. Usage: /tokenstein-today [--currency=usd|eur]"
---
Call the `tokenstein_today` tool from the `claude-tokenstein` MCP server. If $ARGUMENTS contains `--currency=eur`, pass `currency: "eur"`; otherwise omit it. Display the result.
